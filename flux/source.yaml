apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: demo-repo
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://github.com/tmwaas/aws-cloud-infra-project  
  ref:
    branch: main
